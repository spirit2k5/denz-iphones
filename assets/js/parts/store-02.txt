oin(''):'<div class="empty">No phones match this filter.</div>';$$('.product-card',el).forEach(bindCard)}
function initGrids(){
 $$('[data-product-grid]').forEach(grid=>{let cat=grid.dataset.category,featured=grid.dataset.featured==='true',limit=Number(grid.dataset.limit||0);let base=PRODUCTS.filter(p=>(!cat||p.category===cat)&&(!featured||p.featured));if(limit)base=base.slice(0,limit);renderGrid(grid,base);let scope=grid.closest('[data-shop-scope]')||document,search=$('[data-search]',scope),filters=$$('[data-filter]',scope);const run=()=>{let q=(search?.value||'').trim().toLowerCase(),active=filters.find(x=>x.classList.contains('active'))?.dataset.filter||'all';let list=PRODUCTS.filter(p=>!cat||p.category===cat).filter(p=>!q||`${p.name} ${p.condition} ${p.battery} ${p.network} ${p.notes}`.toLowerCase().includes(q));if(active!=='all')list=list.filter(p=>{let f=p.name.includes('Pro Max')?'max':p.name.includes('Pro')?'pro':p.name.includes('Plus')?'plus':'standard';return f===active});renderGrid(grid,list)};search?.addEventListener('input',run);filters.forEach(b=>b.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));b.classList.add('active');run()}))});
}
function initProduct(){
 const host=$('[data-product-detail]');if(!host)return;const id=new URLSearchParams(location.search).get('id'),p=PRODUCTS.find(x=>x.id===id)||PRODUCTS[0];if(!p)return;
 let storage=p.storageOptions?.find(s=>p.prices?.[s])||p.storageOptions?.[0]||'Confirm with Denz';let color=p.colors?.[0]?.name||'As shown / confirm with Denz';let selectedImages=imagesFor(p,color),selectedImage=selectedImages[0]||'';
 const showImage=src=>{selectedImage=src||'';const main=$('[data-main-image]',host);if(main&&selectedImage){main.src=selectedImage;main.alt=`${p.name} ${color}`};$$('[data-thumb]',host).forEach(t=>t.classList.toggle('active',t.dataset.thumb===selectedImage))};
 const renderThumbs=()=>{const wrap=$('[data-color-thumbs]',host);if(!wrap)return;wrap.innerHTML=selectedImages.map((im,i)=>`<button class="thumb ${im===selectedImage?'active':''}" data-thumb="${im}"><img src="${im}" alt="${p.name} ${color} view ${i+1}"></button>`).join('');$$('[data-thumb]',wrap).forEach(t=>t.addEventListener('click',()=>showImage(t.dataset.thumb)))};
 const selectColor=(nextColor,image)=>{color=nextColor;selectedImages=imagesFor(p,color);selectedImage=selectedImages.includes(image)?image:(selectedImages[0]||'');$$('[data-color]',host).forEach(b=>b.classList.toggle('active',b.dataset.color===color));const label=$('[data-color-label]',host);if(label)label.textContent=color;renderThumbs();showImage(selectedImage);syncWa()};
 const autoVideo=p.category==='cheaper-options';
 let media=p.video?`<div class="main-video"><video class="${autoVideo?'cheap-autoplay-video':''}" ${autoVideo?'autoplay muted loop playsinline preload="auto" disablepictureinpicture':'controls playsinline'} poster="${p.poster||''}"><source src="${p.video}" type="video/mp4"></video><span>Video supplied for this stock unit</span></div>`:`<div class="main-product-image"><img data-main-image src="${selectedImage||'assets/img/brand/denz-logo.jpg'}" alt="${p.name} ${color}"></div><div class="thumbs" data-color-thumbs></div><details class="all-photos"><summary>View all ${p.images?.length||0} supplied photos</summary><div class="all-photo-grid">${(p.images||[]).map((im,i)=>`<button data-all-thumb="${im}"><img src="${im}" alt="${p.name} supplied photo ${i+1}" loading="lazy"></button>`).join('')}</div></details>`;
 host.innerHTML=`<div class="gallery">${media}</div><div class="detail-panel"><span class="badge ${catClass(p.category)}">${p.categoryLabel}</span><h1>${p.name}</h1><p class="detail-sub">${p.condition}. ${p.notes||''}</p><div class="detail-price" data-detail-price>${p.prices?.[storage]?money(p.prices[storage]):price(p)}</div>
 <div class="option-block"><div class="option-title"><strong>Storage</strong><span data-storage-label>${storage}</span></div><div class="storage-options">${(p.storageOptions||[]).map(s=>`<button class="storage-btn ${s===storage?'active':''}" data-storage="${s}">${s}${p.prices?.[s]?` · ${money(p.prices[s])}`:''}</button>`).join('')}</div></div>
 ${p.colors?.length?`<div class="option-block"><div class="option-title"><strong>Colour</strong><span data-color-label>${color}</span></div><div class="color-options">${p.colors.map(c=>`<button class="color-btn ${c.name===color?'active':''}" data-color="${c.name}"><span class="swatch" style="--swatch:${c.hex}"></span><span>${c.name}</span></button>`).join('')}</div><p class="option-note">Selecting a colour changes the actual phone image shown above.</p></div>`:`<div class="stock-colour-note"><strong>Colour</strong><span>Specific stock unit. Confirm the colour shown in the supplied stock media with Denz.</span></div>`}
 <div class="spec-grid"><div class="spec"><small>Battery</small><strong>${p.battery||'Confirm'}</strong></div><div class="spec"><small>Network</small><strong>${p.network||'Unlocked'}</strong></div><div class="spec"><small>Warranty</small><strong>${p.warranty||'Confirm'}</strong></div><div class="spec"><small>Free extras</small><strong>Charger + case + protector</strong></div></div>
 ${p.network!=='Unlocked'||/scratch|replaced|eSIM|cycles|battery/i.test(p.notes||'')?`<div class="listing-note"><strong>Important listing note</strong><p>${p.notes||p.network}</p></div>`:''}
 <button class="add-cart" data-add-cart>Add to cart</button><a class="whatsapp-buy" data-wa-buy target="_blank" rel="noreferrer">Order on WhatsApp</a></div>`;
 if(!p.video){renderThumbs();$$('[data-all-thumb]',host).forEach(t=>t.addEventListener('click',()=>{const image=t.dataset.allThumb,matchedColor=p.colors?.find(c=>p.colorImages?.[c.name]?.includes(image))?.name;if(matchedColor)selectColor(matchedColor,image);else showImage(image)}))}
 $$('[data-storage]',host).forEach(b=>b.addEventListener('click',()=>{storage=b.dataset.storage;$$('[data-storage]',host).forEach(x=>x.classList.remove('active'));b.classList.add('active');$('[data-storage-label]',host).textContent=storage;$('[data-detail-price]',host).textContent=p.prices?.[storage]?money(p.prices[storage]):'Ask Denz';syncWa()}));
 $$('[data-color]',host).forEach(b=>b.addEventListener('click',()=>selectColor(b.dataset.color)));
 const syncWa=()=>{let pr=p.prices?.[storage]?money(p.prices[storage]):'price to confirm';$('[data-wa-buy]',host).href=wa(`Hi Denz, I want the ${p.name} (${p.categoryLabel}), ${storage}, colour: ${color}. Listed price: ${pr}. Please confirm stock.`)};syncWa();
 $('[data-add-cart]',host).addEventListener('click',()=>{let cart=getCart(),hero=p.video?(p.poster||''):selectedImage;cart.push({uid:Date.now()+'-'+Math.random(),id:p.id,name:p.name,category:p.categoryLabel,storage,color,price:p.prices?.[storage]||null,image:hero});setCart(cart);toast(`${p.name} added to cart`);openDrawer()});document.title=`${p.name} | Denz iPhones`;
}
function detailsForm(id='drawer'){return `<form class="checkout-form" data-checkout-form="${id}"><div class="form-grid"><label>Full name *<input name="name" required autocomplete="name"></label><label>WhatsApp number *<input name="phone" required inputmode="tel" autocomplete="tel" placeholder="e.g. 082 123 4567"></label><label>Alternate phone<input name="altphone" inputmode="tel"></label><label>Email<input name="email" type="email" autocomplete="email"></label></div><fieldset><legend>How do you want the phone? *</legend><label class="radio"><input type="radio" name="fulfilment" value="Delivery" required> Delivery</label><label class="radio"><input type="radio" name="fulfilment" value="Collection" required> Collection</label></fieldset><div data-address-fields class="address-fields"><label>Street address *<input name="address" autocomplete="street-address"></label><div class="form-grid"><label>Suburb *<input name="suburb" autocomplete="address-level2"></label><label>City *<input name="city" value="Johannesburg" autocomplete="address-level2"></label><label>Province *<input name="province" autocomplete="address-level1"></label><label>Postal code *<input name="postal" required inputmode="numeric" autocomplete="postal-code"></label></div></div><label>Order notes<textarea name="notes" rows="3" placeholder="Anything Denz should know"></textarea></label><button class="checkout-wa" type="submit">Send order to WhatsApp</button><p class="form-help">Delivery has a flat R100 fee. Enter your delivery address before sending the order.</p></form>`}
function orderMessage(cart,data){let lines=cart.map((x,i)=>`${i+1}. ${x.name} (${x.category}) - ${x.storage}, ${x.color}${x.price?` - ${money(x.price)}`:''}`);let itemsTotal=cart.reduce((sum,x)=>sum+(Number(x.price)||0),0);let deliveryFee=data.fulfilment==='Delivery'?100:0;let grandTotal=itemsTotal+deliveryFee;let customer=[`Name: ${data.name}`,`WhatsApp: ${data.phone}`,data.altphone?`Alternate phone: ${data.altphone}`:'',data.email?`Email: ${data.email}`:'',`Method: ${data.fulfilment}`,data.fulfilment==='Delivery'?`Address: ${data.address}, ${data.suburb}, ${data.city}, ${data.province} ${data.postal}`:'Collection at Denz iPhones',data.notes?`Notes: ${data.notes}`:''].filter(Boolean);let totals=[`Items total: $