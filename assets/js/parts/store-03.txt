{money(itemsTotal)}`,data.fulfilment==='Delivery'?`Delivery: ${money(100)}`:'Delivery: R0',`Order total: ${money(grandTotal)}`];return `Hi Denz, I want to place this order:\n\n${lines.join('\n')}\n\nCustomer details:\n${customer.join('\n')}\n\n${totals.join('\n')}\n\nPlease confirm stock and payment details.`}
function bindCheckout(scope=document){$$('[data-checkout-form]',scope).forEach(form=>{if(form.dataset.checkoutBound==='1')return;form.dataset.checkoutBound='1';let addr=$('[data-address-fields]',form);const update=()=>{let delivery=$('input[name="fulfilment"]:checked',form)?.value==='Delivery';addr.classList.toggle('hidden',!delivery);['address','suburb','city','province','postal'].forEach(n=>{let el=$(`[name="${n}"]`,form);if(el)el.required=delivery});let help=$('.form-help',form);if(help)help.textContent=delivery?'Delivery fee: R100 flat rate.':'Collection: no delivery fee.'};$$('input[name="fulfilment"]',form).forEach(r=>r.addEventListener('change',update));update();form.addEventListener('submit',e=>{e.preventDefault();let cart=getCart();if(!cart.length){toast('Your cart is empty');return}if(!form.reportValidity())return;let data=Object.fromEntries(new FormData(form).entries());window.open(wa(orderMessage(cart,data)),'_blank')})})}
function drawerHtml(){return `<div class="cart-overlay" data-cart-overlay></div><aside class="cart-drawer" data-cart-drawer aria-label="Shopping cart"><div class="drawer-head"><div><small>YOUR DENZ CART</small><h2>Ready when you are.</h2></div><button class="drawer-close" data-cart-close aria-label="Close cart">×</button></div><div class="drawer-scroll"><div data-drawer-list></div><div class="drawer-total"><span>Known total</span><strong data-drawer-total>R0</strong></div><details class="drawer-checkout"><summary>Enter order details</summary>${detailsForm('drawer')}</details><a href="cart.html" class="full-cart-link">Open full cart page</a></div></aside>`}
function initDrawer(){document.body.insertAdjacentHTML('beforeend',drawerHtml());$$('[data-cart-open],.cart-btn').forEach(b=>b.addEventListener('click',e=>{if(b.tagName==='A')e.preventDefault();openDrawer()}));$('[data-cart-close]')?.addEventListener('click',closeDrawer);$('[data-cart-overlay]')?.addEventListener('click',closeDrawer);bindCheckout($('[data-cart-drawer]'));renderDrawer()}
function openDrawer(){document.body.classList.add('cart-open');renderDrawer()}function closeDrawer(){document.body.classList.remove('cart-open')}
function renderCartItems(target,totalTarget){let cart=getCart();if(!target)return;target.innerHTML=cart.length?cart.map((x,i)=>`<div class="cart-line"><div class="cart-line-img">${x.image?`<img src="${x.image}" alt="${x.name}">`:''}</div><div><strong>${x.name}</strong><span>${x.storage} · ${x.color}</span><b>${x.price?money(x.price):'Price to confirm'}</b></div><button data-remove-cart="${i}">Remove</button></div>`).join(''):'<div class="empty compact">Your cart is empty.</div>';let total=cart.reduce((s,x)=>s+(x.price||0),0);if(totalTarget)totalTarget.textContent=cart.length?money(total):'R0';$$('[data-remove-cart]',target).forEach(b=>b.addEventListener('click',()=>{let c=getCart();c.splice(Number(b.dataset.removeCart),1);setCart(c);renderFullCart()}))}
function renderDrawer(){renderCartItems($('[data-drawer-list]'),$('[data-drawer-total]'))}
function renderFullCart(){renderCartItems($('[data-cart-list]'),$('[data-cart-total]'))}
function initCartPage(){if(!$('[data-cart-list]'))return;renderFullCart();bindCheckout(document)}
function initPrices(){let body=$('[data-price-body]');if(!body)return;body.innerHTML=PRODUCTS.filter(p=>p.category==='brand-new').flatMap(p=>{let e=Object.entries(p.prices||{});return e.length?e.map(([s,v])=>`<tr><td>${p.name}</td><td>${s}</td><td><strong>${money(v)}</strong></td><td><a href="product.html?id=${p.id}">Choose</a></td></tr>`):[`<tr><td>${p.name}</td><td>Available storage</td><td><strong>Ask Denz</strong></td><td><a href="product.html?id=${p.id}">Choose</a></td></tr>`]}).join('')}
function initLayby(){let inp=$('[data-layby-amount]'),out=$('[data-layby-result]');if(!inp||!out)return;const calc=()=>{let n=Number(inp.value||0);out.textContent=n>0?`${money(Math.ceil(n/4))} × 4 months`:'R0'};inp.addEventListener('input',calc);calc()}
function initMap(){let f=$('[data-map-frame]');if(f)f.src=META.mapsEmbed;$$('[data-map-directions]').forEach(a=>a.href=META.maps)}
function initWa(){$$('[data-wa-general]').forEach(a=>a.href=wa('Hi Denz, I am interested in buying an iPhone. Please help me with stock and prices.'))}
document.addEventListener('DOMContentLoaded',()=>{initTheme();initNav();updateCartCount();initWa();initDrawer();initGrids();initProduct();initCartPage();initPrices();initLayby();initMap()});
})();
