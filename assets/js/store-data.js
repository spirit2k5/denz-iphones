window.DENZ_DATA_READY=(async()=>{const x=new XMLHttpRequest();x.open("GET","assets/js/store-data.gz.b64",false);x.send(null);if(!((x.status>=200&&x.status<300)||x.status===0))throw new Error("Failed to load product catalogue ("+x.status+")");const raw=atob(x.responseText.trim());const bytes=new Uint8Array(raw.length);for(let i=0;i<raw.length;i++)bytes[i]=raw.charCodeAt(i);if(typeof DecompressionStream==="undefined")throw new Error("This browser does not support gzip catalogue loading.");const stream=new Blob([bytes]).stream().pipeThrough(new DecompressionStream("gzip"));const code=await new Response(stream).text();(0,eval)(code);})();